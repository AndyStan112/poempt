export type UserInput = {
  subject: string;
  writingStyle: string;
  stanzaStyle: string;
  rhyme: string;
  verses: string;
  mood: string;
};
export type status = 'loading' | 'authenticated' | 'unauthenticated';
